# 🛡️ Xiaomi MiMo PWA 代码安全与隐私合规守卫文档

> **项目准则**：安全与合规是开源项目的生命线。任何向本仓库（包括但不限于 `main`、分支、PR 与 Issue）提交或推送代码的行为，都必须**强制通过本守卫文档的所有检查项**。

---

## 📋 Git 提交前核心检查清单 (Pre-Commit Checklist)

在执行 `git commit` 与 `git push` 前，必须逐项自查并确认以下五大红线：

```
                    ┌──────────────────────────────────────────────┐
                    │      Git 提交前安全与合规守卫门禁流水线      │
                    └──────────────────────┬───────────────────────┘
                                           │
         ┌───────────────────┬─────────────┴───────┬───────────────────┐
         ▼                   ▼                     ▼                   ▼
  【1. 账户隐私排查】  【2. 网络与节点脱敏】  【3. 本地主机路径】  【4. 违法与合规审查】
  • 真实小米 UID      • MagicDNS 专属域名    • /home/<user>/    • 严禁违规有害言论
  • passToken / Cookie• 个人节点全称         • 个人 scratch 目录• 严禁恶意利用载荷
  • 动态 API 密钥     • 固定出口 IP / 私钥   • 必须通用抽象化   • 依赖与协议授权审查
```

### 🔴 红线一：个人身份与账号隐私（零容忍）
- [ ] **小米账号信息脱敏**：
  - 严禁在代码、注释、测试用例或文档中硬编码任何真实的**小米数字账号 UID**（如特定测试数字串等）。
  - 严禁硬编码开发者的真实姓名、特定用户名（如个人真实姓名/昵称）。默认界面应使用通用的 `MiMo User` 或 `MiMo` 占位，所有真实身份必须通过运行期接口动态获取。
  - 严禁提交任何用户手机号、身份证号或私人邮箱。
- [ ] **鉴权凭证与会话 Cookies**：
  - 严禁提交提取自客户端的 `passToken`、SSO 认证 Cookie、`serviceToken`。
  - 严禁提交客户端运行时生成的 `desktop-api.json` 中的 Bearer Token。
  - 严禁提交任何第三方 AI 服务商的 API Key / Access Key。

### 🔴 红线二：网络拓扑与基础设施隐私
- [ ] **Tailscale 私有节点脱敏**：
  - 严禁泄露个人的 Tailscale 完整私有域名与 MagicDNS（如 `*.tail*.ts.net`、个人专属 tailnet 前缀）。
  - 文档与示例中统一使用通用占位符：`<your-machine>.<tailnet>.ts.net`。
- [ ] **网络地址与私钥安全**：
  - 严禁提交特定的家庭宽带公网出口 IP 或带有个人网络拓扑信息的日志。
  - 严禁提交任何 `.pem`、`.key`、`.pfx` 私钥证书或 SSH 密钥对。

### 🔴 红线三：开发机本地绝对路径与环境隔离
- [ ] **主机用户名与私有路径脱敏**：
  - 严禁出现包含具体开发者用户名的硬编码路径（例如 `/home/nelson/...`、`/Users/nelson/...`、`C:\Users\nelson\...`）。
  - 统一使用跨平台通用方式：
    - Python：`os.path.expanduser("~")`、`Path.home()`、相对路径或环境变量。
    - Shell / Systemd：`$HOME`、`%h`、`$(pwd)` 动态推导。
- [ ] **开发工具内部临时目录过滤**：
  - 严禁包含特定 IDE、Agent 或工具链生成的本地临时路径（如 `.gemini/...`、`scratch/...` 等）。

### 🔴 红线四：本地数据库与用户数据过滤
- [ ] **数据库与真实会话隔离**：
  - 严禁将本地生成的 SQLite 数据库（`mimocode.db`）加入版本控制（包含用户的真实对话、代码历史与文件路径）。
  - 严禁提交 LevelDB 本地存储目录（`Local Storage/leveldb`）。
  - 严禁提交临时文件上传目录（`mimo-uploads`）及其中的真实图片与文档。
  - 确保 `.gitignore` 完整覆盖此类本地数据。

### 🔴 红线五：法律法规与内容合规性审查
- [ ] **法律法规红线（绝对红线）**：
  - 严格遵守中华人民共和国网络安全法及相关法律法规。
  - 严禁包含任何危害国家安全、泄露国家秘密、颠覆国家政权、破坏国家统一的内容。
  - 严禁包含任何宣扬恐怖主义、极端主义、民族仇恨、涉黄涉暴或侵犯他人合法权益的内容。
- [ ] **无恶意行为代码**：
  - 本项目为官方客户端的辅助移动网关，严禁包含任何形式的特洛伊木马、隐蔽反弹 Shell、未声明的端口扫描、拒绝服务（DoS）或未授权数据外传代码。
- [ ] **开源许可与版权合规**：
  - 严格遵循 MIT 开源协议。
  - 严禁在仓库中直接包含未经授权的第三方商业专有闭源二进制文件或专有资产。

---

## 🛠️ 自动化防御与 Pre-Commit 拦截

为避免人为疏忽，项目提供了自动化扫描拦截脚本与 Git Hook 机制：

### 1. 手动执行安全扫描
在准备提交前，随时可以执行：
```bash
./scripts/security-check.sh
```
该脚本会自动对当前工作区及暂存区（Staged files）进行正则扫描，检查是否存在高危特征（真实 UID、硬编码路径、敏感 Token、私有 Tailnet 域名等）。

### 2. 启用 Git Pre-Commit 自动守卫拦截
执行以下命令一键将安全检查挂接为 Git 原生 Hook：
```bash
./scripts/install-hooks.sh
```
启用后，每次执行 `git commit` 时都会自动触发审查：**一旦发现潜在隐私或敏感信息，Git 提交将被强制中止，并给出具体文件与告警行号！**

---

## 🤖 专属仓库管家（AI Pair Assistant）守则

作为常驻本对话的 **mimo-pwa 专属仓库管家**，在此立下守卫誓约：

1. **提交前主动审查**：在任何一次发起 `git add`、`git commit` 或 `git push` 前，必须以本守卫清单作为最高指导准则，主动对所有代码变更（Diff）执行敏感信息与合规性审查。
2. **零容忍拦截**：发现任何个人隐私、具体硬件主机路径、私有网络域名或非合规内容，必须在本地立即脱敏修正，绝不带入提交。
3. **持续迭代防护**：随着功能演进，持续完善 `.gitignore` 与安全检测规则，保障仓库始终干爽、透明、安全。
